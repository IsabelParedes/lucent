{
  "name": "lucent",
  "version": "0.0.0",
  "private": true,
  "description": "Lucent - browser runtime that boots R.wasm in a web worker and drives Shiny over the r-httpuv transport.",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "comlink": "^4.4.2",
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
